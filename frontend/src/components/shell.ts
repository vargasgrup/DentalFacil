/** Shared chrome height — sidebar brand strip + topbar must match for a continuous border. */
export const SHELL_HEADER_CLASS =
  "flex h-20 shrink-0 items-center border-b border-slate-200/90 bg-white";

/** Desktop/tablet sidebar rail width (keep in sync with AppShell padding). */
export const SHELL_SIDEBAR_WIDTH = "w-64";

